# Taste
- User's main project (PathlyAI) is an Angular frontend + ASP.NET Core (.NET 10) API. Publish-log evidence: the API is deployed by Visual Studio's Publish flow to Azure App Service (pathly-web-api…southafricanorth-01.azurewebsites.net, Windows/IIS hosting via web.config + AzureAppServices site extension), with production config in App Service environment variables (e.g. Jwt__Key, double-underscore naming) and dev config in user secrets — this supersedes the earlier "Render via Docker/Linux container" belief for the API, so don't debug the API against Linux-Docker constraints anymore. The committed appsettings.json holds empty placeholder values (Jwt:Key, Groq, Paystack, AzureOpenAI) and is overwritten onto Azure on every publish, so secrets must live in App Service env vars — a missing/empty Jwt__Key makes every request 500. Confidence: 0.95
- Develops on Windows with Visual Studio; the local API is often left running, which locks Debug build outputs. Verify changes with `dotnet build -c Release` (separate output folders) instead of fighting file locks. Confidence: 0.7
- "Ready" means the test suite passes, not just a clean build — when the user asks if changes are ready to test, run `dotnet test` (the "Pathly Tests" project) first and report the pass count before answering. Confidence: 0.6
- Heredocs don't work in this shell — for multi-line git commit messages, write the message to a temp file and commit with `git commit -F <file>`. Confidence: 0.8
- Git commands can exceed the default 30s shell timeout — pass explicit longer timeouts and verify with `git log`/`git status` whether the command actually landed before retrying. Confidence: 0.6
- Leave committing to the user: keep changes uncommitted, say so when summarizing work, and wait for an explicit go-ahead before committing/pushing (the user then deploys to Render). Confidence: 0.6
- Treats provider rate limits/quotas (TPM, plan quotas) as a first-class design concern. For rate-limited third-party AI APIs (e.g. Groq), prefers spreading load across a pool of multiple API keys — rotate the starting key per request (round-robin) and fail over to the next key immediately on 429 or unusable/truncated responses — rather than sleeping/retrying on a single key until its quota is exhausted. Confidence: 0.8
- Structures the ASP.NET Core solution as many small per-layer projects (Enums, Models, DTOs, Data, Interfaces, Helper, Repository, Core, Services, Utility + the PathlyAI_API host) rather than folders inside one project — put new code in the matching layer project instead of piling it into the API host. Confidence: 0.8
ack on every path — even unmapped ones — it means middleware-level failure (e.g. empty Jwt:Key throwing IDX10703 inside the JWT bearer options), not a controller/DB issue. Confidence: 0.7
- Prefers fail-fast, explicit configuration validation over silent runtime degradation: when a required setting (e.g. Jwt:Key) is missing/empty, the app should fail loudly with a clear, actionable error message naming the exact config key and where to set it (user secrets in dev, env var in prod), not serve opaque 500s on every request. Confidence: 0.6
